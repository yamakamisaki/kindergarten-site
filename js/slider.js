$(function() {
  $('.sliders').slick({
      dots: true, //インジケーターを表示 
      autoplay: true, //自動再生 
      autoplaySpeed: 2000, //自動再生の速度 
      fade: true, //フェードイン
      infinite: true //無限ループ
  });
});